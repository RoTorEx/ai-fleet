# Tasks

## Support multiple subscriptions per provider

- Allow one provider to have multiple configured subscriptions/accounts.
- Show friendly names in the menu, for example `Codex 1`, `Codex 2`, or a user-defined name.
- In settings, let the user bind a subscription/account email to a custom display name.
- Keep mixed and per-provider statistics readable when several accounts belong to the same provider.
